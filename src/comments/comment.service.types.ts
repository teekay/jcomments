/** Types generated for queries found in "src/comments/comment.service.ts" */

/** Query 'ThisConnectionMany' is invalid, so its result is assigned type 'never' */
export type IThisConnectionManyResult = never;

/** Query 'ThisConnectionMany' is invalid, so its parameters are assigned type 'never' */
export type IThisConnectionManyParams = never;

